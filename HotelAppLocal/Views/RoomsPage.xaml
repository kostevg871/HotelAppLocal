<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HotelAppLocal.Views.RoomsPage"
             Title="Номера">

    <!-- Основной фон страницы -->
    <ContentPage.BackgroundColor>#EFF6FF</ContentPage.BackgroundColor>

    <Grid RowDefinitions="Auto,*">

        <!-- Верхняя панель с заголовком и кнопкой обновления -->
        <Grid Grid.Row="0"
              Padding="16,20"
              BackgroundColor="#1D4ED8">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <VerticalStackLayout Grid.Column="0" Spacing="4">
                <Label Text="HotelApp"
                       FontSize="24"
                       FontAttributes="Bold"
                       TextColor="White" />

                <Label Text="Выберите доступный номер и забронируйте его"
                       FontSize="13"
                       TextColor="#DBEAFE" />
            </VerticalStackLayout>

            <Button Grid.Column="1"
                    Text="Обновить"
                    Padding="14,6"
                    Margin="10,0,0,0"
                    BackgroundColor="White"
                    TextColor="#1D4ED8"
                    CornerRadius="8"
                    FontAttributes="Bold"
                    Command="{Binding LoadCommand}" />
        </Grid>

        <!-- Список номеров (занимает всё оставшееся место, скроллится) -->
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Rooms}"
                        ItemsLayout="VerticalList"
                        Margin="16,12,16,16">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame HasShadow="True"
                           CornerRadius="12"
                           Padding="14"
                           Margin="0,0,0,10"
                           BackgroundColor="White"
                           BorderColor="#E5E7EB">

                        <Grid ColumnDefinitions="*,Auto"
                              RowDefinitions="Auto,Auto,Auto">

                            <!-- Номер и цена -->
                            <VerticalStackLayout Grid.Row="0"
                                                 Grid.ColumnSpan="2"
                                                 Spacing="2">
                                <Label Text="{Binding Number, StringFormat='Номер {0}'}"
                                       FontSize="18"
                                       FontAttributes="Bold"
                                       TextColor="#111827" />
                                <Label Text="{Binding PricePerNight, StringFormat='{}{0:N0} ₽ / ночь'}"
                                       FontSize="14"
                                       TextColor="#2563EB" />
                            </VerticalStackLayout>

                            <!-- Статус -->
                            <HorizontalStackLayout Grid.Row="1"
                                                   Grid.ColumnSpan="2"
                                                   Margin="0,8,0,0"
                                                   Spacing="6"
                                                   VerticalOptions="Center">
                                <BoxView WidthRequest="8"
                                         HeightRequest="8"
                                         CornerRadius="4"
                                         Color="#22C55E" />
                                <Label Text="Доступен"
                                       FontSize="12"
                                       TextColor="#6B7280" />
                            </HorizontalStackLayout>

                            <!-- Кнопка бронирования -->
                            <Button Grid.Row="2"
                                    Grid.ColumnSpan="2"
                                    Text="Забронировать"
                                    Margin="0,10,0,0"
                                    BackgroundColor="#2563EB"
                                    TextColor="White"
                                    CornerRadius="8"
                                    Padding="10,6"
                                    FontSize="14"
                                    FontAttributes="Bold"
                                    CommandParameter="{Binding .}"
                                    Clicked="OnBookClicked" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>
